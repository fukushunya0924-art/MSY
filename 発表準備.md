# 発表準備（数理生物学会 2026, 9/7–10・口頭）

> 前提：**口頭15分（確定, 2026-07-29）**。講演12分＋質疑3分として骨子を組んである。
> 主軸：**構造定理（密度依存なしLVでは内点MSYが原理的に存在しない）** を核に据える。
> 提出済み要旨（タイトル：多種資源動態モデルを用いたレジーム別最大持続生産量(MSY)の推定）との整合は §1 で確認済み。
> **2026-07-29 改訂**：S4にマイワシ版を追加／S6に変数数の設計ラダー（12・10・8変数）を明記／
> 旧S13(Catch-MSY制約)・旧S14(rの意味不整合)を推定パートの直後（新S8・S9）へ前倒し／
> レジーム別パラメータ比較を新S10として追加／構造定理の導出を §5 に新設（成立条件の誤りを1点修正）。
>
> **2026-07-30 改訂（方針変更）**：**Volterra の原理を「主張」として出さない。**
> 「自種の漁獲が自種の平衡量を変えない」は古典的既知事実であり、これを発表の売りにすると
> Q1（＝既知の再発見では？）に自分から誘導してしまう。
> **主張するのは「このモデルクラスでは平衡収量が f について多重線形 ⇒ 最適 f は常に探索箱の頂点
> ⇒ 報告される MSY は f_max の人工物」という、MSY計算枠組みへの帰結の方**。
> Volterra への言及はスライド上では**脚注1行の出典表記のみ**とし、質疑で問われたときだけ §5 を展開する。

---

## 1. 提出済み要旨との整合チェック

要旨で約束したことは全てスライドで回収する。構造定理は要旨最終文「その適用に必要なパラメータ制約上の条件を明らかにする」の**答え**として提示するので、追加ではなく回収である。

| 要旨の記述 | 対応スライド | 状態 |
|---|---|---|
| 黒潮大蛇行/非大蛇行をレジームとして扱う | S2, S4 | ✅ 既存 |
| 4種二部グラフ型LVのパラメータをレジーム別に推定 | S5, S6, S7 | ✅ 既存 |
| 定数漁獲圧を導入し一定期間シミュレーション | S11 | ✅ 既存 |
| 持続性条件を満たす範囲で期間平均漁獲量を最大化 | S11, S14 | ✅ 既存 |
| 単一種Catch-MSYからの情報導入を試みた | S8 | ✅ 既存 |
| 変換効率に制約を課すと適合度が低下 | S8 | ✅ 既存 |
| 両モデルにおけるパラメータの生物学的意味の比較 | S9, S10 | ✅ 既存 |
| **レジーム別MSYの計算枠組みと、適用に必要な条件を明らかにする** | **S12–S15（構造定理）＋ S16** | ✅ ここが主軸 |

**注意**：要旨には「MSYが存在しない」とは書いていない。発表では「枠組みを構築した結果、その枠組みが返す量の正体が判明した」という**発見の順序どおり**に話す。要旨との齟齬ではなく、要旨が予告した条件解明の帰結として提示すること。

---

## 2. スライド骨子（1枚＝1メッセージ）

各枚の「言いたいこと」は口頭でそのまま言える1文にしてある。◎=絶対必要 ○=標準 △=時間が無ければ削る

**構成の流れ**：問い（S3）→ モデルと推定（S4–S7）→ **単一種情報を入れようとして壊れた話**（S8–S10）→ MSY計算（S11–S12）→ **なぜそうなるかの構造定理**（S13–S15）→ まとめ（S16）。
旧構成では Catch-MSY の話が定理の後（旧S13–S14）にあり「終わった後に戻る」形だったが、
**推定の直後に置くことで「パラメータをどう決めるか」→「決められない」→「そもそも決めても最大が無い」と一本道になる**。

### S1 タイトル ◎（15秒）
- タイトル・氏名・所属。

### S2 背景：黒潮レジームと日本近海の資源変動 ◎（45秒）
- **メッセージ**：日本近海の資源は環境変動と種間相互作用の両方を受け、黒潮大蛇行(LM)/非大蛇行(NLM)で状況が変わる。
- 図：レジーム区分（NLM 2006–2016 / LM 2017–2024）を帯で示した資源量時系列。
- 言い方の注意：**因果を断定しない**。「大蛇行が原因で」ではなく「レジームで分けて比較する」。（多年代レジームシフト＝PDO等との交絡が未検証のため。質疑Q6参照）

### S3 問題設定：MSYはどこから来るのか ◎（60秒）★伏線
- **メッセージ**：単一種Schaeferでは密度依存（環境収容力K）が収量の頭打ちを作り、そのおかげで内点最大 F_MSY=r/2 が存在する。では**密度依存を持たない多種相互作用系でMSYはどう定義されるのか**——これが本研究の問い。
- 図：Schaefer の収量曲線（山型）を1つ描き、「この山を作っているのは K である」と明示。
- **式を1行だけ出しておく**（S13で効かせるため）：
  `B*(f) = K(1 − f/r)` → `Y(f) = f·B*(f) = K f − (K/r) f²`
  → **f² の項があるから山になる。この f² は「自分の漁獲が自分の平衡量を下げる」ことから来ている。**
- ここで伏線を張っておくと S13 の定理が効く。

### S4 対象・データ ◎（45秒）★マイワシ版を追加
- **メッセージ**：被食者2種×捕食者2種の組を、**種構成を替えた2セット**で解析している（結論の頑健性のため）。
- 解析1（**マアジ版**）：被食者 = マアジ(x1)・ウルメイワシ(x2) ／ 捕食者 = ブリ(y1)・サワラ(y2)。
- 解析2（**マイワシ版**）：被食者 x1 を**マイワシ**に差し替えた版。捕食者は同じ。
  - 経緯を1行で：マイワシは単一種Catch-MSYが標準の終端レンジ[0.01,0.4]で解けず例外採用が必要だったため、主解析の被食者x1をマアジに替えた（Phase 11）。
  - **後半の結論（S12–S15）はマイワシ版でも同じ**＝上限張り付き・正の平衡なし。→ 種構成に依存しない。
- 資源量・漁獲量：FRA資源評価（マアジ1982–2024／マイワシ／ブリ／サワラ）＋ e-stat 太平洋12県（ウルメイワシ）。いずれも太平洋系群。
- レジーム：NLM 11年（2006–2016）/ LM 8年（2017–2024）。
- 漁獲圧 f = 漁獲量/資源量（データ由来の**既知の外生強制項**、上限0.95）と明言しておく（質疑Q5対策）。
- 図の作り方：マアジ版を大きく、マイワシ版は小さく脇に置く（「replicate がある」ことだけ伝わればよい）。

### S5 モデル：4種二部グラフ型 Lotka–Volterra ◎（60秒）★伏線の本体
- **メッセージ**：被食者2×捕食者2の二部グラフ型LV。**種内競争項（密度依存）も、同一ギルド内の相互作用も含まない**。
- 式（正規化空間、各種を全期間平均で除す）：
  ```
  dx_i/dt = (r_xi − f_xi) x_i − Σ_j L_ij x_i y_j
  dy_j/dt = (−r_yj − f_yj) y_j + Σ_i (変換効率) L_ij x_i y_j
  ```
- 一般化LV表現 `dB_i/dt = B_i(ρ_i + Σ_j A_ij B_j)` を併記し、**A が「ブロック反対角」**であることを赤字で示す：
  ```
        x1  x2   y1    y2
  x1 [  0   0  −L11  −L12 ]
  x2 [  0   0  −L21  −L22 ]      A = [ 0    −L  ]
  y1 [C1L11 D1L21  0    0  ]          [ C∘L   0  ]
  y2 [C2L12 D2L22  0    0  ]
  ```
  - **対角が0** ＝ 種内競争・自己制限なし
  - **左上ブロックが0** ＝ 被食者どうしの競争なし
  - **右下ブロックが0** ＝ 捕食者どうしの相互作用なし
- **口頭で必ず言う**：「この**ゼロの配置**が、後半の結論をすべて決めます」。
  （※後で効くのは「対角が0」だけではなく**ブロック構造全体**。詳細は §5）

### S6 パラメータ推定 ○（50秒）★変数数の設計ラダーを明記
- 正規化空間でLSODA積分 → 対数残差を least_squares(trf) で最小化。
- 局所解対策：マルチスタート×マルチシード（NLM 64×12 / LM 32×8）、LMのみ L2正則化 λ=0.005。
- 評価指標：NRMSE 主体（R²は分散が小さい系列で暴れるため補助）。
- **推定は「自由度を段階的に落とす3水準」で実施した**（この表を出しておくと S8 が説明不要になる）：

  | 版 | 自由変数 | 固定するもの | ねらい |
  |---|---|---|---|
  | **自由推定** | **12変数** | なし | モデルの表現力の上限を見る |
  | 制約（rのみ） | **10変数** | r_x1, r_x2（Catch-MSY 由来） | 被食者の増殖率だけ単一種情報で固定 |
  | 制約（r+S） | **8変数** | r_x1, r_x2 ＋ 変換効率の和 S1=c1+d1, S2=c2+d2 | Catch-MSY 由来情報を最大限入れる |

  - 12変数の内訳：r_x1, r_x2, r_y1, r_y2, L11, L12, L21, L22, C1, D1, C2, D2
  - 補足：**種内競争項ありの16変数版も試したが不採用**。LM期8点に対し過剰で識別性が崩壊し非物理値（c1=41等）が出たため（Q2・Q7の伏線）。
- 一言：「以降で示すフィットは 12変数の自由推定。8/10変数の結果は S8 で比較する。」

### S7 推定結果（フィット） ◎（45秒）
- 図：`msy/outputs/マアジ版/自由推定/fit_マアジ_ウルメイワシ_ブリ_サワラ_capacity_ry.png`
- 数値（12変数自由推定）：NLM 平均NRMSE **0.099**（平均R²=+0.71）／ LM 平均NRMSE **0.065**（平均R²=+0.34）。
- 一言：「両レジームとも観測を良く再現している」＝**以降の議論は当てはまりの悪さのせいではない**と釘を刺す。
- （R²がLMで低いのは分散の小さい系列のアーティファクト。ブリLMが R²=−1.14 なのはこれ。聞かれたら答える程度に留める。）

### S8 単一種Catch-MSYからの情報導入とその限界 ◎（75秒）★要旨の約束を回収／旧S13を前倒し
- **メッセージ**：単一種推定から多種モデルへ移植できる情報は**被食者の増殖率 r_x に限られる**。変換効率の和まで固定すると適合が壊れる。
- 表（平均NRMSE, NLM/LM）— S6 の設計ラダーがそのまま結果表になる：

  | 推定 | NLM | LM |
  |---|---|---|
  | 自由推定（12変数） | 0.099 | 0.065 |
  | r_x のみ固定（10変数） | 0.293 | 0.170 |
  | r_x + 変換効率の和 S も固定（8変数） | 0.452 | 0.338 |

- **固定するパラメータを増やすほど単調に悪化する**、という向きで読ませるのが要点。
- 理由：Catch-MSYが与えるのは変換効率の**和** c+d だが、資源量データが実質的に決めているのは捕食圧との**積** C×L。和を固定すると積を保つために L が代償的に歪む（例：L21 が NLM で 0.224 → 1.00）。L は被食者式にも現れるため、歪みが被食者の当てはまりに波及する。
- **表現の注意**：「ODEは積しか識別しない」と断定しない。原理的には L は被食者式から識別可能で、実際には**実用上ほぼ縮退している（ill-conditioned）**というのが正確。質疑Q3対策。

### S9 パラメータの生物学的意味の不一致 ○（45秒）★要旨の約束を回収／旧S14を前倒し
- **メッセージ**：単一種モデルの r と多種モデルの r_x は**同じ記号だが別の量**。だから S8 の固定は失敗すべくして失敗した。
- 事実：マアジだけ「固定した r_x1 = 0.228 < 実測漁獲圧 f_x1 ≈ 0.41」。被食者式の実効内因成長 (r_x1 − f_x1) = −0.18/年 が捕食項の前から負になり、モデルが構造的に暴落させる（マアジのNRMSEだけ 0.485/0.452 と悪い）。
- 対照：ウルメは f≈0.11 ≪ r_x2=0.739 で健全（LM 0.089）。
- 解釈：Catch-MSYの r は**密度依存を含む余剰生産曲線**の内因成長、ODEの r_x は**密度依存なし**の純内因成長。**移植には意味の翻訳が要る**。
- ここで一言足すと S13 につながる：「単一種モデルの r には密度依存が畳み込まれている。その密度依存こそ、この後で問題になるものです。」

### S10 レジーム別パラメータの比較 ○（50秒）★新規追加
- **メッセージ**：NLM→LM でパラメータは「被食者の回転が速くなり、捕食者2種の立場が入れ替わる」向きに動く。ただし**点推定のみなので定性的な読みに留める**。
- まず**データそのもの**（期間平均資源量, 千トン）を1行で出す — 解釈の錨にする：

  | | マアジ x1 | ウルメ x2 | ブリ y1 | サワラ y2 |
  |---|---|---|---|---|
  | NLM 平均 | 64.4 | 243.5 | 289.7 | 4.82 |
  | LM 平均 | 46.3 | 203.6 | 365.0 | 7.13 |
  | 変化 | −28% | −16% | **+26%** | **+48%** |

  → **LMでは被食者2種が減り捕食者2種が増える**、という素直な事実。

- 推定パラメータ（正規化空間, 12変数自由推定）：

  | 記号 | 意味 | NLM | LM | LM/NLM | 読み |
  |---|---|---|---|---|---|
  | r_x1 | マアジ 内因成長 | 0.735 | 2.639 | ×3.6 | LMで高回転。ただし**過大**（Q7へ） |
  | r_x2 | ウルメ 内因成長 | 0.280 | 0.445 | ×1.6 | 同方向・穏当 |
  | r_y1 | ブリ 自然死亡 | 0.177 | 0.036 | ×0.20 | LMでブリは死ににくい |
  | r_y2 | サワラ 自然死亡 | 0.048 | 0.336 | ×7.0 | LMでサワラは高回転（速く死に速く増える） |
  | L11 | マアジ→ブリ 捕食圧 | 0.125 | 1.157 | ×9.3 | **1超は過大**（Q9：年68%消失相当） |
  | L12 | マアジ→サワラ | 0.281 | 1.093 | ×3.9 | 同上 |
  | L21 | ウルメ→ブリ | 0.070 | 0.399 | ×5.7 | 上昇 |
  | L22 | ウルメ→サワラ | 0.055 | 0.014 | ×0.25 | **低下** |

- **C は単独で語らない**（S8の非識別性のため）。捕食者の実効増加率は**積 C×L** で示す：

  | 経路 | NLM | LM | 読み |
  |---|---|---|---|
  | マアジ→ブリ (C1·L11) | 0.305 | 0.230 | やや低下 |
  | ウルメ→ブリ (D1·L21) | 0.272 | 0.120 | 低下 |
  | マアジ→サワラ (C2·L12) | 0.271 | **0.686** | **2.5倍に増加** |
  | ウルメ→サワラ (D2·L22) | 0.169 | **0.0025** | **ほぼ消失** |

- **1文でまとめる口頭**：「LMではサワラの取り分がウルメイワシからマアジへ乗り換わり、ブリは捕食への依存を下げつつ自然死亡が下がって増える——と読めます。」
- **必ず添える留保（3点）**：
  1. **点推定のみでCIが無い**。増減の有意性は主張しない（Q4）。
  2. LMは**8年×4種=32点で12変数**。r_x1=2.64 や L11=1.16 は生物学的に過大で、過剰適合の兆候（Q7）。
  3. C と L は積でしか実質識別されないので、**C単独・L単独の増減ではなく積で議論する**（S8）。
- 位置づけ：これは記述であって主張ではない。**本発表の結論（S13）はこれらの値に依存しない**と明言してから次へ渡す。

### S11 MSYの計算手続き ◎（45秒）
- 推定モデルに魚種別の定数漁獲圧ベクトル f=(f_x1,f_x2,f_y1,f_y2) を注入 → T年シミュレーション。
- 持続性条件を満たす f の中で、期間平均漁獲量 Y = (1/T)∫ Σ f_i B_i dt を最大化（4次元グリッド探索、上限 f_max=0.95）。
- 持続性条件は4モードを実装（S14で触れる）。既定は終端値 B_i(T) ≥ 0.9·B_i(0)。
- **留保（未確認, 2026-08-18追記）**：この判定は $T$=レジーム長（NLM約11年／LM約8年）**1点だけ**を見ており、**同じ $f$ を $T$ を超えて続けた場合に90%が保たれ続けるかは検証していない**。密度依存なしLVの内部平衡は中立安定になりうる（Q13）うえS14の通り無漁獲平衡自体が負なので、「$T$年だけ持続に見えて実は長期的に崩壊/発散する」経路を排除できていない。→ Q15参照。
- **留保（種間スケール合算問題）**：Σ_i f_i B_i は絶対スケール（千トン）での単純和なので、種ごとの資源量規模差がそのまま重みになる。期間平均資源量（1994–2024）はブリ257／ウルメイワシ197／マアジ85千トンに対しサワラは4.4千トンと2桁小さく、サワラの直接寄与は最大でも f_y2×4.4≈4.2千トン（合計の1%未満）に埋もれる。実質的に規模の大きい3種（ブリ・ウルメ・マアジ）の合算に近く、f_y2が最適解に効くとしても自身の収量としてではなく(C∘L)⁻¹経由でマアジ・ウルメの平衡量を動かす間接効果である可能性が高い（→ Q14）。

### S12 結果1：最適漁獲圧が「箱の角」に張り付く ◎（60秒）
- **メッセージ**：最適 f はグリッドの上限に張り付き、上限自体を上げると収量が際限なく増える。
- 図：上限感度（f_upper 0.25→1.25 で収量 206.7→380.6→607.2→801.4→**1094.0** 千トン/年、単調増加）＋グリッド散布図。
- **具体例（マアジ版・自由推定, 8⁴=4096点グリッド, F_MAX=0.95）**：LM の制約付き最適解は
  `f*=[f_maaji, f_urume, f_buri, f_sawara] = [0.271, 0.0, 0.543, 0.95]`。
  4成分すべてが `np.linspace(0, 0.95, 8)` の**格子点そのもの**（丸め誤差ではない厳密値）であり、
  ウルメイワシは**下限0（無漁獲）**、サワラは**上限0.95**に張り付く＝「角」は上限側だけでなく
  下限側にも起きる（多重線形関数の頂点最大なので両方向にありうる）。
- 一言：「これは探索の失敗ではなく、系統的な現象だった」→ S13へ。

### S13 結果2：なぜ角に行くのか＝平衡収量が漁獲圧について「多重線形」だから ◎◎（90秒）★★本発表の核
> 導出と説明の練習用の詳細は **§5** に全部書いた。ここはスライドに載せる分だけ。
>
> **⚠️ 出し方の方針（2026-07-30）**：このスライドの主張は **「平衡収量が f について多重線形 ⇒ 最適 f は必ず箱の頂点 ⇒ 報告MSYは f_max の人工物」**。
> その途中に出てくる「自種の漁獲は自種の平衡量を変えない」は**古典的な既知事実であり、本研究の主張ではない**。
> **タイトルにも見出しにも Volterra の名前を出さない。** 出典は最下部の脚注1行（`cf. Volterra 1926`）に留め、
> 「Volterraの原理と呼ばれるものです」と口で言うのは、**質疑で聞かれたときだけ**（Q1）。
> 理由：先に名前を出すと「じゃあ既知ですね」で話が終わり、こちらの帰結（f_max 人工物）が聞かれなくなる。

- **メッセージ**：この モデルでは平衡収量が漁獲圧について1次（多重線形）になるため、**最大値は必ず探索範囲の端に来る**。頭打ちが原理的に生じない。
- **まず2種で直感を渡す**（15秒。ここを飛ばすと伝わらない）：
  ```
  dx/dt = x(r − f_x − a y)   → 平衡条件 r − f_x − a y = 0  … y* を決める（x は消える）
  dy/dt = y(−m − f_y + b x)  → 平衡条件 −m − f_y + b x = 0 … x* を決める（y は消える）
  ⇒ x* = (m+f_y)/b ,  y* = (r−f_x)/a
  ```
  **x* の式に f_x が入っていない**。自分の漁獲が自分の平衡量を動かさない。
  **なぜか**：自己制限項 (−α x) が無いので、被食者の「増減ゼロ条件」から x 自身が消えてしまうから。
  （S3の Schaefer では −α x があるので x* が f に依存し、収量に f² が出て山ができた。）
- **4種でも同じ**。平衡は2つの2×2線形系に分解される：
  ```
  L·y_eq       = r_x − f_x   →  y_eq は f_x にのみ依存（f_y に依存しない）
  (C∘L)·x_eq   = r_y + f_y   →  x_eq は f_y にのみ依存（f_x に依存しない）
  ```
- **命題**：一般化LVで内部平衡が存在するとき `B_eq(f) = B_eq(0) + A⁻¹f`（f について**アフィン**）であり、
  平衡収量 `Y_eq(f) = Σ_i f_i B_eq,i(f) = f·B_eq(0) + fᵀA⁻¹f`。
  本モデルは **A がブロック反対角 ⇒ A⁻¹ もブロック反対角 ⇒ diag(A⁻¹)=0** なので f_i² の項が消え、
  `Y_eq(f)` は **f について多重線形（各成分について高々1次）**。
  多重線形関数の箱 [0, f_max]⁴ 上の最大は必ず**頂点**で達成される。
  → **内点に厳密な最大（＝MSY）は存在しない。**
- **系**：報告される「MSY」は探索上限 f_max の選び方に完全に依存する人工物。
- 数値検証（フィット済み4種、既存コードで確認）：

  | 検証項目 | NLM | LM |
  |---|---|---|
  | ∂x_eq/∂f_x | 厳密に 0 | 厳密に 0 |
  | ∂y_eq/∂f_y | 厳密に 0 | 厳密に 0 |
  | Y_eq の各 f_j 方向の2階差分 | 4.3e−12 | 1.4e−12（機械精度＝線形） |
  | 頂点16点 vs 内部20万点の最大 | 内点は頂点を超えず | 同左 |

- **正直に言う留保**：この命題は平衡収量についての厳密な結果。実際の計算は有限T の時間平均収量＋持続性制約なので厳密な頂点性は保証されないが、数値実験（S12）は同じ構造を示す。
- **スライド最下部の脚注（小さく1行）**：`平衡量が自種の漁獲に依存しない点は古典的に知られる（cf. Volterra 1926）。本研究の主張はそこから従う「最適 f の頂点性」の方。`
- **口頭で絶対に言わないこと**：
  1. 「対角が0だから A⁻¹ の対角も0」。**これは一般には偽**。効いているのはブロック構造（§5・Q11参照）。
  2. 「これは Volterra の原理です」（＝自分から新規性を消す。聞かれてから答える）。

### S14 結果3：そもそも正の共存平衡が存在しない ○（60秒）
- **メッセージ**：フィット済みパラメータでは無漁獲平衡に負の成分が出る（NLM: マアジ −50.0千トン／LM: ウルメ −130.2千トン）。
- 表：4パターン（マアジ/マイワシ × NLM/LM）の det と平衡値（`行列式結果.md` の総括表）。
- 帰結：平衡比を基準とする持続性判定（equilibrium_lrp）は**適用不可**。実運用は終端値基準（legacy）と長期軌道下限（trajectory_floor）に頼らざるを得ない。
- 種構成をマイワシ→マアジに替えても NLM の結論は不変＝**頑健**（S4のマイワシ版がここで効く）。
- **S13との論理関係を必ず1文で言う**（教授が最初に突くのはここ）：
  「S13は『**仮に**正の内部平衡があっても内点MSYは無い』という主張。実際のフィットはさらに厳しく、その平衡すら正でない。どちらの意味でも内点MSYは定義できない。」
- **言い過ぎ注意（2026-08-05訂正）**：「正の共存平衡が存在しない」は**$f=0$（無漁獲）でのみ**確認した事実。頂点16点で符号を確認したところ、$f\neq0$では正になる頂点が存在する（NLM 1/16点、LM 4/16点＝1つの面全体）。→ 口頭では「**無漁獲**平衡は負」と明言し、「共存平衡はどこにも存在しない」と言い切らない。ただし、この正の頂点が実際にfeasible（持続性制約を満たす軌道）かどうかは未確認＝**future work**（§4タスク5c）。

### S15 結果4：一律漁獲努力にすると内点ピークが「立ったり立たなかったり」する △（45秒）
- **メッセージ**：4種すべてに同じ f を課すと1次元問題になり内点ピークが現れるが、**NLMでは立ちLMでは立たない**（レジームで反転）。
- 表：マアジNLM f*=0.373（161.1）／マアジLM f*=0.95張り付き（232.9）／マイワシNLM f*=0.228／マイワシLM 張り付き。
- 解釈：この山は密度依存が作るものではなく「全種同一f」という**拘束**と栄養段階カップリングが作る見かけの最大。種の重み付け（等重み＝恣意的）に依存するため生物学的不変量ではない。
- 位置づけ：主たるMSY定義には**採らない**。S13の主張を補強する傍証。

### S16 まとめ ◎（60秒）
1. レジーム別・多種相互作用を考慮したMSY計算の枠組みを構築し、実データで動かした（要旨の約束）。
2. その結果、この枠組みが返す量は MSY ではなく「**資源下限制約下の最大収量**」だと判明した。原因は**密度依存の欠如**であり、平衡収量が漁獲圧について多重線形になるため最適点が必ず探索上限に来る——**推定値の良し悪しに依存しない構造的帰結**である。
3. 単一種情報の移植は r_x に限定すべきで、変換効率の和の固定は識別性の壁に阻まれる。**固定するパラメータを増やすほど適合は単調に悪化する**（12→10→8変数）。
4. 今後：(a) 内点MSYを定義するには種内競争項の再導入が必要。(b) 種ごとの資源量スケールが2桁異なるため、単純トン合算の目的関数では規模の大きい種（ブリ・ウルメ・マアジ）が支配的になり**サワラ**の寄与が埋もれる問題があり、種ごとに正規化した目的関数（例：期間平均資源量で無次元化した収量の合算）での再計算を検討する（Q14）。

### 予備スライド（質疑用）
- B1 4つの持続性判定モードの定義表（`MSY.md` §5）
- B2 平衡点の手計算（`行列式結果.md` の4パターン）
- B3 Catch-MSY の事前分布と終端レンジ感度（マイワシが標準レンジで解けない件）
- B4 マイワシ版との比較（種構成を替えても結論不変・S4/S14の裏付け）
- B5 推定予算の感度（NLM 64×12 / LM 32×8 の根拠）
- B6 f の算出方法と 0.95 上限の扱い
- **B7（新規）構造定理の完全導出**：A⁻¹ のブロック公式・`diag(A)=0` では足りない反例・多重線形性から頂点最大まで（§5 に本文あり）。**Q1（Volterraの原理として既知では？）が出たときに初めて出すスライド。**

### 時間配分と削る順
- 上記16枚を全部やると **約14分**（S1–S16 の想定秒数の合計 845秒＋遷移）。**12分に収めるには2分削る必要がある。**
- **12分実走プラン（推奨）**：
  1. **S15 を落とす**（−45秒）→ 予備スライドへ移動。質疑で「一律漁獲努力なら山が立つのでは」と来たら出す。
  2. **S6 を30秒に圧縮**（−20秒）：最適化手法の詳細は口頭で1文、**変数数の表だけ残す**（S8で使うため表は必須）。
  3. **S10 を35秒に圧縮**（−15秒）：期間平均資源量の行と C×L の表だけ残し、12変数の全表は予備へ。
  4. **S2/S4 を各10秒圧縮**（−20秒）。
  → 合計 −100秒 ≒ 745秒（12分25秒）。遷移込みで実測しながら S14 をさらに30秒削る余地を持っておく。
- **絶対に削らない**：S3（伏線）・S12（現象）・S13（定理）・S16（まとめ）。S3を削るとS13が理解されない。

---

## 3. 想定質疑と回答方針

| # | 想定質問 | 回答方針 |
|---|---|---|
| Q1 | それは Volterra の原理として既知では？ | **「はい、そこは既知です」と即座に認める**（本文中では主張していないので、ここで折れても失うものが無い設計にしてある）。そのうえで「**本研究が示したのはその先で、密度依存なしのモデルでMSYを数値的に求めると、出てくる答えが探索上限 f_max の設定そのものになってしまう**という、MSY計算枠組みの側の帰結です」と返す。既存研究（May et al. 1979; Legović et al. 2010 等）は多種系で単一種MSYが同時達成できないことを示したが、本研究は実データ推定パラメータ上で「最適漁獲圧が探索上限の人工物になる」ことを定量的に示した。→ B7 を出す。**要文献確認（§4のタスク1）** |
| Q2 | 種内競争項を入れれば済む話では？ | その通りで、それが結論。ただし12→16変数となりLM期8点では過去に非物理値（c1=41等）で識別性が崩壊した実績があるため、データ長が本質的制約。 |
| Q3 | 変換効率と捕食圧は本当に識別不能なのか | 原理的には識別可能（Lは被食者式に単独で現れる）。実際には情報量が乏しく縮退している。→ ヘッセ行列の固有構造で示せる（§4のタスク6） |
| Q4 | 推定パラメータの不確実性は？ | 現状は点推定のみ。ただし**主結論（内点MSY非存在）はパラメータ値に依存しない構造的結果**なので、CIの有無で崩れない。レジーム差を論じる部分（S10）にはCIが必要で、そこは定性的な記述に留めていると明言する。 |
| Q5 | 漁獲圧 f を外生で与えてよいのか | f = 漁獲量/資源量で、資源量はVPA出力。よって f の誤差は資源量の誤差と相関する。この点は限界として明示する。 |
| Q6 | レジーム差は本当に黒潮由来か（PDO等との交絡） | 因果は主張しない。分割年をずらす置換検定で「この分割が特別か」は検証可能（§4のタスク7）。 |
| Q7 | LM期8点で12変数は過剰では？ | 観測は8年×4種=32点。正則化λ=0.005を入れているが、r_x1=2.64・L11=1.16 のような非物理値が出ており、過剰適合は**認める**。これが制約導入（S8）の動機だった。**そしてこの過剰適合は主結論を変えない**（構造的結果のため）。 |
| Q8 | マアジで f > r となるのは推定の誤りでは？ | 推定値ではなくデータ由来（f=漁獲量/資源量）。単一種Schaeferの r（余剰生産）と多種ODEの r_x（純内因成長）の意味の違いに起因する（S9）。 |
| Q9 | 正規化空間での L の値の意味は？ | 各種が平均1に規格化されているので L·y は被食者の年あたり捕食死亡率（瞬間率）にほぼ相当。健全レンジは概ね 0.1–0.3。L11(LM)=1.16 は年68%消失に相当し過大＝Q7の過剰適合の現れ。 |
| Q10 | 「資源下限制約下の最大収量」は管理に使えるのか | 上限 f_max の設定に依存する量なので、そのまま管理基準には使えない。使うなら f_max を生物学的根拠（例：漁具・努力量の物理的上限）で設定する必要がある。 |
| **Q11** | **「対角が0」だけで内点最大が消えるのか？** | **消えない。必要なのは diag(A⁻¹)=0。** 本モデルは A がブロック反対角（ギルド内相互作用ゼロ）なので A⁻¹ もブロック反対角になり、そこから diag(A⁻¹)=0 が従う。被食者間競争を入れると diag(A)=0 のままでも diag(A⁻¹)≠0 になり、内点最大が復活しうる。→ §5・B7 |
| **Q12** | **正の内部平衡が無いのに平衡収量の定理を語る意味は？** | 定理は「**仮に**正の内部平衡があっても内点MSYは無い」という上界の議論。**無漁獲平衡**は実際に正でない（S14）。ただし$f\neq0$では正になる頂点も見つかっており（2026-08-05）、「共存平衡がどこにも無い」とまでは言い切らない。どちらの場合でも定理自体（多重線形→頂点最大）は成立する、というのが主張。 |
| **Q13** | **密度依存なしLVの平衡は中立安定では？「平衡収量」に意味があるのか** | ご指摘の通り、自己制限が無いLVの内部平衡は中立安定（周回軌道）になりうる。だから平衡収量は**形式的な参照量**であり、実際の評価は有限Tの時間平均収量で行っている（S11）。両者が同じ構造を示すことは数値で確認した（§4のタスク5）。 |
| **Q14** | **複数種の漁獲量をトンで単純合算してよいのか（規模の大きい種に支配されないか）** | その通りで、実データで確認済みの限界。**期間平均資源量（1994–2024, `data_loader.get_series()` から算出）はブリ257.4／ウルメ196.6／マアジ84.9千トンに対しサワラは4.35千トンと2桁小さく**、Σf_iB_iの単純和では**サワラ**の直接寄与（最大でも0.95×4.35≈4.1千トン、全体の1%未満）がほぼ無視できる。サワラの漁獲圧f_y2が最適解に効くとしても、それは自身の収量としてではなく(C∘L)⁻¹を介してマアジ・ウルメの平衡量を動かす間接効果としてであり、生態学的解釈には注意が要る。対策として、種ごとに正規化した目的関数（例：各種の期間平均資源量で割った無次元収量の合算）での再計算を今後の課題とする（S16）。<br>**訂正（2026-08-08）**：旧記述「マアジ1617／ウルメ1143／サワラ1020／ブリ10千トン」は出典不明の誤値で、桁が2つ小さいのは**ブリではなくサワラ**。レジーム別の平均は NLM: マアジ64.4／ウルメ243.5／ブリ289.7／サワラ4.82、LM: 46.3／203.6／365.0／7.13（S10の表と一致）。 |
| **Q15** | **90%持続制約は $T$ 年しか見ていない。その後も持続する保証はあるのか** | **ない、というのが正直な答え。** 現行の判定（mode1=legacy_path, endpoint）は $T$=レジーム長（NLM約11年／LM約8年）における終端値1点だけを見ており、その $f$ を延長した場合の長期挙動は未検証。密度依存なしLVの内部平衡は中立安定になりうる（Q13）ため、$T$年間は90%を保っていても、その先で振動が続く・ゆっくり崩れる可能性を理論上は排除できない。トラジェクトリ下限モード（mode3, trajectory_floor）はburn-in 100年+評価100年の長期窓で確認しようとしたが、無漁獲平衡が求まらないため基準値が長期シミュ平均へのフォールバックになり、実際に負の資源量比が出るケースもあった＝**むしろ長期化すると崩れる兆候**。この点は今回のスコープでは詰め切れておらず、$T$年の窓に限定した結果である旨を明示する。 |

---

## 4. 9/7 までのタスク（今日 7/29 から約5.7週間）

主結論が**構造的でパラメータに依存しない**ため、追加の重い再推定は不要。優先度は「定理の足場固め」＞「レジーム差の主張の補強」。

### 第1週（7/29–8/4）— 定理の足場固め【最優先】
1. **文献確認**：Volterra の原理／多種系MSY（Volterra 1926 Nature; May, Beddington, Clark, Holt & Laws 1979 Science 205:267; Legović, Klanjšček & Geček 2010 Ecol. Modelling; press-perturbation の ∂B*/∂ρ = −A⁻¹ は Bender, Case & Gilpin 1984 Ecology）を実際に読む。**目的が変わった点に注意**：以前は「差分を主張するため」だったが、Volterra を主張に使わない方針にしたので、**目的は「どこまでが既知かを正確に線引きし、Q1で即座に『そこは既知です』と言い切れるようにすること」**。特に「密度依存なしモデルでの数値MSY探索が f_max の人工物になる」という**帰結の側**が先行研究で既に書かれていないかを確認する（書かれていたら発表の主張を組み直す必要があるので最優先）。
2. **定理を自分の手で再導出**：§5 を見ずに、白紙から
   （i）2種での x*=(m+f_y)/b → （ii）A⁻¹ のブロック公式 → （iii）Y_eq の多重線形性 → （iv）頂点最大、
   を書けるようにする。**特に「diag(A)=0 では足りない／diag(A⁻¹)=0 が要る」を自分の言葉で説明できることがゴール**（Q11）。
3. **教授へ報告**：(a) 主軸を構造定理に格上げする件、(b) 要旨との整合（§1の表）、(c) **Volterra の原理を主張として出さない方針**とその理由（既知の再発見に見えるリスク回避、主張は f_max 人工物の側）、(d) §5.3 の成立条件の訂正（対角0では足りない）。**§5をそのまま説明の台本に使う。**

### 第2週（8/5–8/11）— 数値の再現性と限界の明示
4. **検証コードの本採用**：平衡収量の構造検証（∂x_eq/∂f_x=0・多重線形性・頂点性）を `現行コード/msy/` の正式スクリプトに昇格させ、`docs/research_log.md` に Phase 14 として記録。スライドの数値表（S13）はここから引く。**あわせて「diag(A⁻¹)を実際に計算して0であることを出力する」テストを1本入れる**（Q11の根拠を数値でも持つため）。
5. **時間平均収量版の挙動確認**：厳密な多重線形性は平衡収量の性質。有限T の時間平均収量が f についてどれだけ非線形かを数値で測り、留保の言い方（S13末尾・Q13）を確定する。
5b. ~~共存平衡の正値性を箱全体で確認~~ **完了（2026-08-05）**：頂点16点で符号確認した結果、**箱全体では負ではない**（NLM 1/16点、LM 4/16点＝1面で全種正）。「正の共存平衡が存在しない」は$f=0$限定の主張に訂正（S14・Q12・§5.8.5に反映済み）。
5c. **【future work】正の共存平衡を持つ$f$の領域と、実際にfeasible（持続性制約を満たす実軌道）な$f$の領域が重なるかを突き合わせる**（2026-08-05追加）：`grid_search_general`等が実際にfeasibleと判定した$f$集合と、`build_A_rho`→`equilibrium_generalized_lv`で正と判定される$f$集合を並べて比較する。S12の「最適解」がこの正領域と重なるかは未確認。優先度は低（主結論の多重線形性定理は共存平衡の有無に関わらず成立するため）だが、質疑で突かれたら「確認中」と答えられるようにしておく。
5d. **【future work / Q15対策】mode1(legacy_path, endpoint)の90%制約は $T$=レジーム長（8〜11年）の終端1点しか見ておらず、$T$を超えて同じ$f$を続けた場合に持続するかは未検証**（2026-08-18追加）。mode3(trajectory_floor)をburn_in=100年・評価窓=100年で試したところ、無漁獲平衡が求まらず基準値が長期シミュ平均へのフォールバックになった上、負の資源量比（NLM x2で-0.0055〜0.11等）が出るケースがあり、むしろ長期化すると崩れる兆候が見える。優先度は中：構造定理（S13）自体はこの論点に依存しないが、S11–S12の「持続性制約」という言葉の意味を問われたとき（Q15）に「$T$年の窓に限定した結果」と明言できる根拠として、mode3の再実行結果（`run_sustainability_diagnostics.py`）を1枚精査しておく。

### 第3週（8/12–8/18）— 補強解析（余力に応じて）
6. **識別性の定量化**（Q3対策）：推定解でのヘッセ行列 or 残差ヤコビアンの特異値分解を取り、(C, L) → (C·λ, L/λ) 方向がほぼゼロ特異値であることを示す。図1枚で「和 vs 積」の主張（S8）が数学的に固まる。
7. **分割年の置換検定**（Q6対策）：分割年を 2006–2024 の各年に動かしてNRMSEやパラメータ差の分布を作り、2017分割の特異性を見る。
8. **S10 の数値の出典固定**：レジーム別パラメータ表は `estimates_capacity_ry.pkl`（12変数自由推定）から生成しているので、**表を吐くスクリプトを1本置いて再現可能にする**。手打ち転記は事故のもと。

### 第4週（8/19–8/25）— スライド作成
9. 図の発表用作り直し（文字サイズ・日本語ラベル・1図1メッセージ）。既存PNGは情報量が多すぎるので分解する。
10. S4 のマイワシ版比較図（小さく脇に置く用）と S5 の A 行列の「ゼロ配置」図を新規作成。
11. スライド初版を通しで作る。

### 第5週（8/26–9/1）
12. 通し練習（**12分厳守**：§2末尾の12分実走プランで削った状態で計測）＋ 教授レビュー ＋ 想定問答の詰め。

### 第6週（9/2–9/6）
13. 修正・予備スライド整備（B7を含む）・リハーサル2回。

### やらないこと（明示的に切る）
- 種内競争項（16変数）の再導入 → LM期8点では識別性が崩壊する実績があり、5週間では危険。Future Work として明言する。
- ブートストラップCI → 主結論に不要。S10 は「CIが無いので定性的読みに留める」と明言して回避する。

---

## 5. 構造定理：教授に説明するための完全な導出

> **このセクションの位置づけ（2026-07-30）**
> - **発表で主張するのは 5.5 だけ**：平衡収量が f について多重線形 ⇒ 最大は箱の頂点 ⇒ 報告MSYは f_max の人工物。
> - **5.1–5.2 は既知の古典（Volterra 1926）**であり、**主張ではなく前提**。スライドでは名前を出さず、
>   「自種を獲っても自種の平衡量が動かない」という**モデルの性質**として淡々と示す（S13の方針欄参照）。
> - このセクション自体は**教授への説明用・質疑用の台本**なので、既知部分も含めて全部書いてある。
>
> **旧記述の訂正**：旧S10の結論（多重線形→頂点最大）は**正しい**が、理由づけに1点誤りがあった。
> 「**A の対角が0（種内競争なし）だから**多重線形になる」は**不十分**。
> 正しくは「**A⁻¹ の対角が0だから**多重線形になる」で、本モデルではそれが
> **A のブロック反対角構造（ギルド内相互作用がゼロ）**から従う。
> `diag(A)=0` から `diag(A⁻¹)=0` は**一般には導けない**（5.3の反例参照）。ここは教授に必ず突かれる。

### 5.1 前提として知っておく古典（Volterra 1926）— ★発表では主張しない

D'Ancona の観察：第一次大戦中にアドリア海の漁獲努力が下がったとき、水揚げに占める**捕食魚（サメ・エイ類）の割合が上がった**。なぜか。

Volterra の2種モデル：
```
dx/dt = x (r − a y)        x: 被食者, y: 捕食者
dy/dt = y (−m + b x)
```
内部平衡（x>0, y>0）は各括弧を0にして
```
r − a y = 0  →  y* = r / a
−m + b x = 0 →  x* = m / b
```
**注目点**：`x*` は捕食者側のパラメータ (m, b) だけで決まり、被食者自身のパラメータ (r) を含まない。逆も同様。

ここに「両種を個体数に比例して殺す漁獲」を入れる（r → r − f_x, m → m + f_y）：
```
x* = (m + f_y) / b      … f_y で増える, f_x に依存しない
y* = (r − f_x) / a      … f_x で減る,  f_y に依存しない
```
→ **漁獲すると被食者が増え捕食者が減る**。漁獲をやめれば逆。これが D'Ancona の観察の説明であり、**Volterra の原理**と呼ばれるもの。

**⚠️ ここまでは100年前から既知**。発表でこれを「発見」として語ると Q1 で終わる。**スライドでは名前を出さず、次節の性質だけを「このモデルの構造」として提示する。**

### 5.2 本研究で使うのはその「片側」— ★ここも既知。主張ではなく step

Volterra の原理の通常の言明は上の「両種を獲ると被食者が増える」だが、**本研究で効くのはその根拠になっている次の性質**：

> **自種の漁獲率 f_i は、自種の平衡量 B*_i に一切影響しない。**（∂B*_i/∂f_i = 0）

**なぜそうなるのかの一番わかりやすい説明**（これを教授に言えばよい）：

平衡とは「各種の**1個体あたり**増加率がゼロ」ということ。被食者の1個体あたり増加率は
```
(1/x)·dx/dt = r − f_x − a y
```
で、**ここに x が現れない**。だから「= 0」という式は **y についての方程式**であって、x は決まらない。
つまり**被食者の収支式は、被食者ではなく捕食者の量を決めている**。f_x はこの式の中にあるので、f_x が動かすのは y* であって x* ではない。

**密度依存があるとどうなるか**（S3への接続）：自己制限項 −α x を入れると
```
(1/x)·dx/dt = r − f_x − α x − a y
```
となり、**x が式に現れる**。すると f_x を上げたぶん x* が下がる。この「自分の漁獲で自分が減る」効果があってはじめて、収量 `Y = f·x*(f)` に f² の項が出て**山ができる**。
- Schaefer（捕食者なし）：`x*(f) = K(1 − f/r)` → `Y(f) = Kf − (K/r)f²` → 頂点 f = r/2 が MSY。
- 本モデル（α なし）：`x*` が f_x を含まない → f_x² の項が出ない → **山ができない**。

**この対比がS3→S13の骨格**であり、これだけ言えれば発表としては十分成立する。以下 5.3–5.4 は4種への一般化で、質疑（Q11）用。**5.5 が本研究の主張。**

### 5.3 n種への一般化：どこまでが真か

一般化LV（漁獲込み）：
```
dB_i/dt = B_i ( ρ_i − f_i + Σ_j A_ij B_j ) ,   B = (x1, x2, y1, y2)
```
内部平衡（全成分が正と仮定）：
```
ρ − f + A B* = 0   ⟹   B*(f) = −A⁻¹(ρ − f) = B*(0) + A⁻¹ f
```
- **B\*(f) は f について アフィン（1次）**。これは A が正則でありさえすれば常に成り立つ。
- 感度は `∂B*_i/∂f_j = (A⁻¹)_{ij}`。これは生態学では **press perturbation** の公式として知られる（Bender, Case & Gilpin 1984）。

したがって
> **「自種の漁獲が自種の平衡量を変えない」⟺ (A⁻¹)_{ii} = 0**、すなわち **A の逆行列の対角がゼロ**。

**⚠️ ここが旧記述の誤り**：`diag(A) = 0` は `diag(A⁻¹) = 0` を**含意しない**。
反例（3×3, 対角ゼロ）：
```
A = [[0,1,1],[1,0,1],[1,1,0]]  ⟹  A⁻¹ = ½[[−1,1,1],[1,−1,1],[1,1,−1]]
```
対角は −1/2 ≠ 0。つまり「種内競争が無い」だけでは Volterra の原理は出ない。

### 5.4 本モデルで成り立つ理由：ブロック反対角構造

本モデルの A は、被食者ブロック（2種）と捕食者ブロック（2種）の**間**にしか成分が無い：
```
      x1     x2     y1     y2
x1 [  0      0    −L11   −L12 ]
x2 [  0      0    −L21   −L22 ]        A = [  0    −L  ]
y1 [C1L11  D1L21   0      0   ]            [ C∘L    0  ]
y2 [C2L12  D2L22   0      0   ]
```
- 左上ブロック = 0 ← **被食者どうしの競争を入れていない**（対角0はこの一部）
- 右下ブロック = 0 ← **捕食者どうしの相互作用を入れていない**

このとき、L と C∘L がともに正則なら
```
A⁻¹ = [  0        (C∘L)⁻¹ ]
      [ −L⁻¹       0      ]
```
（検算：`[[0,−L],[C∘L,0]]·[[0,(C∘L)⁻¹],[−L⁻¹,0]] = [[L·L⁻¹, 0],[0, (C∘L)(C∘L)⁻¹]] = I`）

**A⁻¹ もブロック反対角 ⟹ diag(A⁻¹) = 0。** これで 5.3 の条件が満たされる。

**行列を使わない同じ話**（スライドではこちらを出す）：平衡条件を素直に書き下すと4本の式が2組に分かれる。
```
被食者2本： L · y_eq = r_x − f_x          → y_eq を決める（x_eq が入っていない）
捕食者2本： (C∘L) · x_eq = r_y + f_y      → x_eq を決める（y_eq が入っていない）
```
被食者の式に x_eq が現れないのは、被食者どうしの相互作用（自己制限を含む）がゼロだから。
**だから f_x は y_eq だけを、f_y は x_eq だけを動かす。**（2種の古典的性質がそのまま4種に持ち上がっただけで、ここまでは新しくない。）

### 5.5 「内点MSYが無い」までの一歩 — ★★ここが本研究の主張

平衡収量：
```
Y_eq(f) = Σ_i f_i B*_i(f)
        = Σ_i f_i [ B*_i(0) + Σ_j (A⁻¹)_{ij} f_j ]
        = f·B*(0) + fᵀ A⁻¹ f
```
`diag(A⁻¹) = 0` なので二次形式から **f_i² の項がすべて消える**：
```
fᵀ A⁻¹ f = Σ_{i≠j} (A⁻¹)_{ij} f_i f_j
```
残るのは異なる変数どうしの積だけ。したがって `Y_eq` は **多重線形**（＝どの f_i についても、他を固定すれば**直線**）。

**直線なら、区間 [0, f_max] 上の最大は必ず端点。** これを4座標それぞれに順に適用すれば、
箱 [0,f_max]⁴ 上の最大は必ず**頂点（各成分が 0 か f_max）**で達成される。
⟹ **内点に厳密な最大は存在しない。** 報告される「MSY」は f_max の人工物。

**正確な言い方の注意**：多重線形関数が定数になる退化ケースでは内点でも最大値をとりうるので、
「内点最大が存在しない」ではなく「**最大は必ず頂点でも達成される／内点に厳密最大は無い**」と言うのが厳密。

**なぜここが「新しい」と言えるのか**（Q1でこう言う）：5.1–5.4 は古典的に既知の性質。
本研究の主張は、**その性質を持つモデルで数値的にMSYを探索すると、返ってくる最適解が
生物学的な量ではなく「探索箱の設定（f_max）」そのものになってしまう**、という
**MSY計算枠組みの側への帰結**であり、それを実データ推定パラメータ上で定量的に示した点にある。
→ したがって発表では 5.1–5.4 を素通りし、**この段落を主張として立てる**。

### 5.6 教授に突かれる点と答え（先回り）

| 突かれる点 | 答え |
|---|---|
| 「対角が0なら A⁻¹ の対角も0でしょ」 | 一般には偽。5.3の3×3反例を出す。本モデルで成り立つのは**ブロック反対角構造**のおかげ。 |
| 「被食者間競争を入れたらどうなる」 | 左上ブロックが非ゼロになり A⁻¹ はブロック反対角でなくなる → diag(A⁻¹)≠0 → f_i² が復活 → **内点最大が戻りうる**。これが「密度依存を入れれば解決」の数学的な中身（Q2）。 |
| 「正の内部平衡が無いのに平衡の話をするのか」 | 定理は「**仮に**あっても内点最大は無い」という主張。**無漁獲**平衡は正でない（S14）。B*(f) は f のアフィン関数なので、**正になる f の集合は多面体**であり、実際に頂点16点で確認すると $f\neq0$ の一部（NLM1点／LM4点＝1面）は正になる（2026-08-05）。観測されている無漁獲の操業点はその多面体の外側にある、という言い方が正確。 |
| 「α なしLVの平衡は中立安定では」 | その通り。だから平衡収量は形式的な参照量で、実測評価は有限Tの時間平均収量（S11）。両者が同じ構造を示すことは数値で確認する（§4タスク5）。 |
| 「それは Volterra の原理そのもので新規性が無い」 | **争わない。「はい、5.1–5.4 は既知です」と認める。** そのうえで主張は 5.5：**その既知の性質を持つモデルで数値MSY探索をすると出力が f_max の人工物になる**という計算枠組み側の帰結を、実データ推定パラメータ上で定量的に示した点。→ 文献確認（§4タスク1）でこの言い方を固める。**スライド本文で Volterra を主張として出していないので、ここで折れても失うものが無い設計にしてある。** |

### 5.8 有限期間 T への拡張：O(1/T)論法（2026-08-05追加）★教授質疑Q13の根拠固め用

> **位置づけ**：5.5 は平衡収量 $Y_{eq}(f)$ についての厳密な結果。実際の数値探索（S11）が使うのは有限$T$の時間平均収量 $Y_T(f)$ なので、両者のギャップを埋める議論。**5.5と全く同じ前提（共存平衡の存在）の傘の下にある**ことが最後に判明する（5.8.4）。

すべて成分表示（行列を使わない）。$\lambda_{11},\lambda_{12},\lambda_{21},\lambda_{22}$＝捕食圧、$c_1,d_1,c_2,d_2$＝変換効率、$a_1:=r_{x1}-f_{x1}$，$a_2:=r_{x2}-f_{x2}$，$b_1:=r_{y1}+f_{y1}$，$b_2:=r_{y2}+f_{y2}$。

**5.8.1 対数微分の恒等式**（近似なし）
$$
\frac{\dot x_1}{x_1}=a_1-\lambda_{11}y_1-\lambda_{12}y_2,\qquad
\frac{\dot x_2}{x_2}=a_2-\lambda_{21}y_1-\lambda_{22}y_2
$$
$$
\frac{\dot y_1}{y_1}=-b_1+c_1\lambda_{11}x_1+d_1\lambda_{21}x_2,\qquad
\frac{\dot y_2}{y_2}=-b_2+c_2\lambda_{12}x_1+d_2\lambda_{22}x_2
$$

**5.8.2 平衡点（消去法）**：$\Delta_\lambda=\lambda_{11}\lambda_{22}-\lambda_{12}\lambda_{21}$，$\Delta_c=c_1d_2\lambda_{11}\lambda_{22}-c_2d_1\lambda_{12}\lambda_{21}$ として
$$
y_1^*=\frac{a_1\lambda_{22}-a_2\lambda_{12}}{\Delta_\lambda},\quad y_2^*=\frac{a_2\lambda_{11}-a_1\lambda_{21}}{\Delta_\lambda},\quad
x_1^*=\frac{b_1d_2\lambda_{22}-b_2d_1\lambda_{21}}{\Delta_c},\quad x_2^*=\frac{b_2c_1\lambda_{11}-b_1c_2\lambda_{12}}{\Delta_c}
$$

**5.8.3 有限 $T$ の時間平均に同じ消去法を適用**：$(1)$式を$[0,T]$で積分し $T$で割ると（$\bar y_j(T):=\frac1T\int_0^Ty_jdt$，$\varepsilon_1(T):=\frac{\ln x_1(T)-\ln x_1(0)}{T}$）
$$
\lambda_{11}\bar y_1(T)+\lambda_{12}\bar y_2(T)=a_1-\varepsilon_1(T)
$$
これは5.8.2の平衡条件と同形（右辺が $a_1\to a_1-\varepsilon_1(T)$ に変わっただけ）なので、同じ消去計算で
$$
\bar y_1(T)-y_1^*=-\frac{\varepsilon_1(T)\lambda_{22}-\varepsilon_2(T)\lambda_{12}}{\Delta_\lambda},\qquad(\text{同様に }\bar y_2,\bar x_1,\bar x_2)
$$
資源量が有界域 $0<m\le x_i,y_j\le M$ に留まる（persistence）限り $\varepsilon_1(T),\varepsilon_2(T),\delta_1(T),\delta_2(T)=O(1/T)$（周期性や平衡への収束は不要、有界性だけで十分）。したがって
$$
Y_T(f)=f_{x1}\bar x_1(T)+f_{x2}\bar x_2(T)+f_{y1}\bar y_1(T)+f_{y2}\bar y_2(T)=Y_{eq}(f)+O(1/T)
$$

**5.8.4 この論法も「共存平衡が存在すれば」という条件付き**：$\bar x_1(T)$は正の量の時間平均なので必ず正。もし$x_1^*<0$（Phase 14: NLMマアジ$x_1^*=-50$千トン, LMウルメ$x_2^*=-130$千トン、いずれも**無漁獲**平衡で確認）なら「$\bar x_1(T)\to x_1^*$」は成立しえず、前提の有界性（persistence）が崩れているはず（軌道は正の平衡へ収束するのではなく、境界＝0へ向けて流れると考えられる）。
→ **5.8は5.5と同じ「仮に正の共存平衡があれば」という上界の議論**であり、独立な弱点ではない。ただしPhase 14の負値確認は$f=0$（無漁獲）のみ。$B^*(f)$は$f$についてアフィンなので、探索箱$[0,f_{max}]^4$内の**どこかで**正転する可能性は理論上排除できず、実際に頂点16点で確認したところ**正転していた**（2026-08-05、5.8.5参照）。

**5.8.5 頂点16点での符号確認結果（2026-08-05）**：`estimates_capacity_ry.pkl`（12変数自由推定）を使い、全16頂点 $f\in\{0,0.95\}^4$ で $B^*(f)$ を計算した。

| レジーム | 全4種が正になる頂点 | 例（マアジ,ウルメ,ブリ,サワラ, 千トン） |
|---|---|---|
| NLM | 1/16（$f=(0,0,0.95,0.95)$のみ） | (237.1, 2.1, 857.8, 6.3) |
| LM | 4/16（$f_{x2}=0,f_{y1}=0.95$ を固定した1つの面） | 例: (21.4, 1494.2, 390.9, 9.1) |

$B^*$はアフィンなので、LMで面の4隅すべてが正 ⟹ **面全体が正**（アフィン関数の最小は頂点で取るため）。NLMは1頂点のみで正領域はごく狭いと推測される。

**5.8.6 ここで止める（future work）**：この「正の共存平衡を持つ$f$」の集合と、既存の`sustainability.py`が実際に feasible（＝観測初期値から積分した有限T軌道が持続性制約を満たす）と判定する$f$の集合は**別物**（前者は代数的な平衡点の符号、後者は実軌道の閾値判定）。両者が重なるかどうかの突き合わせは今回はやらず、**future workとして§4に切り出す**（タスク5c）。

### 5.7 一言で言うなら

**（発表・教授説明で使う版 — Volterra を出さない）**
> 「このモデルには密度依存がありません。そのせいで、**ある種をどれだけ獲ってもその種の平衡量が動かず**、
> 収量が漁獲圧の1次関数になってしまう。1次関数の最大は端点なので、**最適漁獲圧は必ず探索範囲の端に張り付く**。
> つまり我々が『MSY』として報告していた値は、**探索範囲の上限をどこに置いたかという設定そのもの**でした。
> MSY の『山』を作っていたのは K だったので、K を落としたモデルでは山も一緒に消えていた、ということです。」

**（質疑で Volterra を問われたときの追加1文）**
> 「はい、平衡量が自種の漁獲に依存しない点自体は Volterra が1926年に示した古典的性質です。
> 本研究が申し上げているのは、その性質を持つモデルでMSYを数値探索すると何が出てくるか、という側の話です。」

---

## 参照
- `MSY.md`（4モードの定義・診断結果・総括）
- `行列式結果.md`（平衡点の手計算4パターン）
- `docs/research_log.md` Phase 8–14（実験経緯の一次記録）
- `要旨準備.md`（提出前の検討過程。障害A/B/Cの整理）
- `現行コード/msy/sustainability.py` 冒頭 docstring（A行列とブロック分解の実装上の定義）
- S10の数値の出典：`現行コード/msy/estimates_capacity_ry.pkl`（12変数自由推定, params_norm / means / metrics）
- 図：`現行コード/msy/outputs/マアジ版/`（自由推定・制約10var・制約8var）、`現行コード/catch_msy/outputs/`
