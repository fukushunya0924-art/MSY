# 発表準備（数理生物学会 2026, 9/7–10・口頭）

> 前提：**口頭15分（確定, 2026-07-29）**。講演12分＋質疑3分として骨子を組んである。
> 主軸：**構造定理（密度依存なしLVでは内点MSYが原理的に存在しない）** を核に据える。
> 提出済み要旨（タイトル：多種資源動態モデルを用いたレジーム別最大持続生産量(MSY)の推定）との整合は §1 で確認済み。

---

## 1. 提出済み要旨との整合チェック

要旨で約束したことは全てスライドで回収する。構造定理は要旨最終文「その適用に必要なパラメータ制約上の条件を明らかにする」の**答え**として提示するので、追加ではなく回収である。

| 要旨の記述 | 対応スライド | 状態 |
|---|---|---|
| 黒潮大蛇行/非大蛇行をレジームとして扱う | S2, S4 | ✅ 既存 |
| 4種二部グラフ型LVのパラメータをレジーム別に推定 | S5, S6, S7 | ✅ 既存 |
| 定数漁獲圧を導入し一定期間シミュレーション | S8 | ✅ 既存 |
| 持続性条件を満たす範囲で期間平均漁獲量を最大化 | S8, S11 | ✅ 既存 |
| 単一種Catch-MSYからの情報導入を試みた | S13 | ✅ 既存 |
| 変換効率に制約を課すと適合度が低下 | S13 | ✅ 既存 |
| 両モデルにおけるパラメータの生物学的意味の比較 | S13, S14 | ✅ 既存 |
| **レジーム別MSYの計算枠組みと、適用に必要な条件を明らかにする** | **S9–S12（構造定理）＋ S15** | ✅ ここが主軸 |

**注意**：要旨には「MSYが存在しない」とは書いていない。発表では「枠組みを構築した結果、その枠組みが返す量の正体が判明した」という**発見の順序どおり**に話す。要旨との齟齬ではなく、要旨が予告した条件解明の帰結として提示すること。

---

## 2. スライド骨子（1枚＝1メッセージ）

各枚の「言いたいこと」は口頭でそのまま言える1文にしてある。◎=絶対必要 ○=標準 △=時間が無ければ削る

### S1 タイトル ◎（15秒）
- タイトル・氏名・所属。

### S2 背景：黒潮レジームと日本近海の資源変動 ◎（45秒）
- **メッセージ**：日本近海の資源は環境変動と種間相互作用の両方を受け、黒潮大蛇行(LM)/非大蛇行(NLM)で状況が変わる。
- 図：レジーム区分（NLM 2006–2016 / LM 2017–2024）を帯で示した4種の資源量時系列。
- 言い方の注意：**因果を断定しない**。「大蛇行が原因で」ではなく「レジームで分けて比較する」。（多年代レジームシフト＝PDO等との交絡が未検証のため。質疑Q6参照）

### S3 問題設定：MSYはどこから来るのか ◎（60秒）★伏線
- **メッセージ**：単一種Schaeferでは密度依存（環境収容力K）が収量の頭打ちを作り、そのおかげで内点最大 F_MSY=r/2 が存在する。では**密度依存を持たない多種相互作用系でMSYはどう定義されるのか**——これが本研究の問い。
- 図：Schaefer の収量曲線（山型）を1つ描き、「この山を作っているのは K である」と明示。
- ここで伏線を張っておくと S10 の定理が効く。

### S4 対象・データ ◎（45秒）
- 被食者：マアジ(x1)・ウルメイワシ(x2) ／ 捕食者：ブリ(y1)・サワラ(y2)、いずれも太平洋系群。
- 資源量・漁獲量：FRA資源評価（マアジ1982–2024, ブリ, サワラ）＋ e-stat 太平洋12県（ウルメ）。
- レジーム：NLM 11年 / LM 8年。
- 漁獲圧 f = 漁獲量/資源量（データ由来の**既知の外生強制項**、上限0.95）と明言しておく（質疑Q5対策）。

### S5 モデル：4種二部グラフ型 Lotka–Volterra ◎（60秒）★伏線の本体
- **メッセージ**：被食者2×捕食者2の二部グラフ型LV。**種内競争項（密度依存）は含まない**。
- 式（正規化空間、各種を全期間平均で除す）：
  ```
  dx_i/dt = (r_xi − f_xi) x_i − Σ_j L_ij x_i y_j
  dy_j/dt = (−r_yj − f_yj) y_j + Σ_i (係数) L_ij x_i y_j
  ```
- 一般化LV表現 `dB_i/dt = B_i(ρ_i + Σ_j A_ij B_j)` を併記し、**A の対角がすべて0**であることを赤字で示す。ここが後半のすべての鍵。

### S6 パラメータ推定 ○（45秒）
- 正規化空間でLSODA積分 → 対数残差を least_squares(trf) で最小化。
- 局所解対策：マルチスタート×マルチシード（NLM 64×12 / LM 32×8）、LMのみ L2正則化 λ=0.005。
- 評価指標：NRMSE 主体（R²は分散が小さい系列で暴れるため補助）。

### S7 推定結果（フィット） ◎（45秒）
- 図：`msy/outputs/マアジ版/自由推定/fit_マアジ_ウルメイワシ_ブリ_サワラ_capacity_ry.png`
- 数値：NLM 平均NRMSE **0.099**（R²=+0.71）／ LM 平均NRMSE **0.065**（R²=+0.34）。
- 一言：「両レジームとも観測を良く再現している」＝**以降の議論は当てはまりの悪さのせいではない**と釘を刺す。

### S8 MSYの計算手続き ◎（45秒）
- 推定モデルに魚種別の定数漁獲圧ベクトル f=(f_x1,f_x2,f_y1,f_y2) を注入 → T年シミュレーション。
- 持続性条件を満たす f の中で、期間平均漁獲量 Y = (1/T)∫ Σ f_i B_i dt を最大化（4次元グリッド探索、上限 f_max=0.95）。
- 持続性条件は4モードを実装（S11で触れる）。既定は終端値 B_i(T) ≥ 0.9·B_i(0)。

### S9 結果1：最適漁獲圧が「箱の角」に張り付く ◎（60秒）
- **メッセージ**：最適 f はグリッドの上限に張り付き、上限自体を上げると収量が際限なく増える。
- 図：上限感度（f_upper 0.25→1.25 で収量 206.7→380.6→607.2→801.4→**1094.0** 千トン/年、単調増加）＋グリッド散布図。
- 一言：「これは探索の失敗ではなく、系統的な現象だった」→ S10へ。

### S10 結果2：なぜ角に行くのか＝Volterraの原理 ◎◎（90秒）★★本発表の核
- **メッセージ**：**自種を漁獲しても自種の平衡量は変わらない**（Volterraの原理, 1926）。ゆえに収量は漁獲圧に比例して増え続け、頭打ちが生じない。
- 平衡は2つの2×2線形系に分解される：
  ```
  L·y_eq = r_x − f_x     →  y_eq は f_x にのみ依存（f_y に依存しない）
  (C∘L)·x_eq = r_y + f_y →  x_eq は f_y にのみ依存（f_x に依存しない）
  ```
- **命題**：A の対角が0（種内競争なし）の一般化LVで内部平衡が存在するとき、平衡収量
  `Y_eq(f) = Σ_i f_i B_eq,i(f)` は **f について多重線形（各成分について1次）**。
  多重線形関数の箱 [0, f_max]^4 上の最大は必ず**頂点**で達成される。
  → **内点最大（＝MSY）は原理的に存在しない。**
- **系**：報告される「MSY」は探索上限 f_max の選び方に完全に依存する人工物。
- 数値検証（フィット済み4種、既存コードで確認）：

  | 検証項目 | NLM | LM |
  |---|---|---|
  | ∂x_eq/∂f_x | 厳密に 0 | 厳密に 0 |
  | ∂y_eq/∂f_y | 厳密に 0 | 厳密に 0 |
  | Y_eq の各 f_j 方向の2階差分 | 4.3e−12 | 1.4e−12（機械精度＝線形） |
  | 頂点16点 vs 内部20万点の最大 | 内点は頂点を超えず | 同左 |

- **正直に言う留保**：この命題は平衡収量についての厳密な結果。実際の計算は有限T の時間平均収量＋持続性制約なので厳密な頂点性は保証されないが、数値実験（S9）は同じ構造を示す。

### S11 結果3：そもそも正の共存平衡が存在しない ○（60秒）
- **メッセージ**：フィット済みパラメータでは無漁獲平衡に負の成分が出る（NLM: マアジ −50.0千トン／LM: ウルメ −130.2千トン）。
- 表：4パターン（マアジ/マイワシ × NLM/LM）の det と平衡値（`行列式結果.md` の総括表）。
- 帰結：平衡比を基準とする持続性判定（equilibrium_lrp）は**適用不可**。実運用は終端値基準（legacy）と長期軌道下限（trajectory_floor）に頼らざるを得ない。
- 種構成をマイワシ→マアジに替えても NLM の結論は不変＝**頑健**。

### S12 結果4：一律漁獲努力にすると内点ピークが「立ったり立たなかったり」する △（45秒）
- **メッセージ**：4種すべてに同じ f を課すと1次元問題になり内点ピークが現れるが、**NLMでは立ちLMでは立たない**（レジームで反転）。
- 表：マアジNLM f*=0.373（161.1）／マアジLM f*=0.95張り付き（232.9）／マイワシNLM f*=0.228／マイワシLM 張り付き。
- 解釈：この山は密度依存が作るものではなく「全種同一f」という**拘束**と栄養段階カップリングが作る見かけの最大。種の重み付け（等重み＝恣意的）に依存するため生物学的不変量ではない。
- 位置づけ：主たるMSY定義には**採らない**。S10の主張を補強する傍証。

### S13 単一種Catch-MSYからの情報導入とその限界 ◎（75秒）★要旨の約束を回収
- **メッセージ**：単一種推定から多種モデルへ移植できる情報は**被食者の増殖率 r_x に限られる**。変換効率の和まで固定すると適合が壊れる。
- 表（平均NRMSE, NLM/LM）：

  | 推定 | NLM | LM |
  |---|---|---|
  | 自由推定（12変数） | 0.099 | 0.065 |
  | r_x のみ固定（10変数） | 0.293 | 0.170 |
  | r_x + 変換効率の和 S も固定（8変数） | 0.452 | 0.338 |

- 理由：Catch-MSYが与えるのは変換効率の**和** c+d だが、資源量データが実質的に決めているのは捕食圧との**積** C×L。和を固定すると積を保つために L が代償的に歪む（例：L21 が NLM で 0.224 → 1.00）。L は被食者式にも現れるため、歪みが被食者の当てはまりに波及する。
- **表現の注意**：「ODEは積しか識別しない」と断定しない。原理的には L は被食者式から識別可能で、実際には**実用上ほぼ縮退している（ill-conditioned）**というのが正確。質疑Q3対策。

### S14 パラメータの生物学的意味の不一致 ○（45秒）★要旨の約束を回収
- **メッセージ**：単一種モデルの r と多種モデルの r_x は**同じ記号だが別の量**。
- 事実：マアジだけ「固定した r_x1 = 0.228 < 実測漁獲圧 f_x1 ≈ 0.41」。被食者式の実効内因成長 (r_x1 − f_x1) = −0.18/年 が捕食項の前から負になり、モデルが構造的に暴落させる（マアジのNRMSEだけ 0.485/0.452 と悪い）。
- 対照：ウルメは f≈0.11 ≪ r_x2=0.739 で健全（LM 0.089）。
- 解釈：Catch-MSYの r は密度依存を含む余剰生産曲線の内因成長、ODEの r_x は密度依存なしの純内因成長。**移植には意味の翻訳が要る**。

### S15 まとめ ◎（60秒）
1. レジーム別・多種相互作用を考慮したMSY計算の枠組みを構築し、実データで動かした（要旨の約束）。
2. その結果、この枠組みが返す量は MSY ではなく「**資源下限制約下の最大収量**」だと判明した。原因は密度依存の欠如＝Volterraの原理に由来する構造的帰結であり、**推定値の良し悪しに依存しない**。
3. 単一種情報の移植は r_x に限定すべきで、変換効率の和の固定は識別性の壁に阻まれる。
4. 今後：内点MSYを定義するには種内競争項の再導入か、漁獲率上限の生物学的根拠づけが必要。

### 予備スライド（質疑用）
- B1 4つの持続性判定モードの定義表（`MSY.md` §5）
- B2 平衡点の手計算（`行列式結果.md` の4パターン）
- B3 Catch-MSY の事前分布と終端レンジ感度（マイワシが標準レンジで解けない件）
- B4 マイワシ版との比較（種構成を替えても結論不変）
- B5 推定予算の感度（NLM 64×12 / LM 32×8 の根拠）
- B6 f の算出方法と 0.95 上限の扱い

### 削る順（時間が足りない場合）
S12 → S6（推定手法の詳細を口頭のみに）→ S11（S10の中に1行で吸収）

---

## 3. 想定質疑と回答方針

| # | 想定質問 | 回答方針 |
|---|---|---|
| Q1 | それは Volterra の原理として既知では？ | その通りで、**既知の原理が多種漁業のMSY計算に及ぼす帰結**を定量化した点が貢献。既存研究（May et al. 1979; Legović et al. 2010 等）は多種系で単一種MSYが同時達成できないことを示したが、本研究は「密度依存なしモデルでは最適漁獲圧が探索上限の人工物になる」ことを実データ推定パラメータ上で示した。→ **要文献確認（§4のタスク1）** |
| Q2 | 種内競争項を入れれば済む話では？ | その通りで、それが結論。ただし12→16変数となりLM期8点では過去に非物理値（c1=41等）で識別性が崩壊した実績があるため、データ長が本質的制約。 |
| Q3 | 変換効率と捕食圧は本当に識別不能なのか | 原理的には識別可能（Lは被食者式に単独で現れる）。実際には情報量が乏しく縮退している。→ ヘッセ行列の固有構造で示せる（§4のタスク3） |
| Q4 | 推定パラメータの不確実性は？ | 現状は点推定のみ。ただし**主結論（内点MSY非存在）はパラメータ値に依存しない構造的結果**なので、CIの有無で崩れない。レジーム差を論じる部分にはCIが必要（§4のタスク4）。 |
| Q5 | 漁獲圧 f を外生で与えてよいのか | f = 漁獲量/資源量で、資源量はVPA出力。よって f の誤差は資源量の誤差と相関する。この点は限界として明示する。 |
| Q6 | レジーム差は本当に黒潮由来か（PDO等との交絡） | 因果は主張しない。分割年をずらす置換検定で「この分割が特別か」は検証可能（§4のタスク5）。 |
| Q7 | LM期8点で12変数は過剰では？ | 観測は8年×4種=32点。正則化λ=0.005を入れているが、r_x1=3.0 のような非物理値が出た実績があり、過剰適合は認める。これが制約導入の動機だった。 |
| Q8 | マアジで f > r となるのは推定の誤りでは？ | 推定値ではなくデータ由来（f=漁獲量/資源量）。単一種Schaeferの r（余剰生産）と多種ODEの r_x（純内因成長）の意味の違いに起因する（S14）。 |
| Q9 | 正規化空間での L の値の意味は？ | 各種が平均1に規格化されているので L·y は被食者の年あたり捕食死亡率（割合）にほぼ相当。健全レンジは概ね 0.1–0.3 で、L21=1.00 は年63%消失に相当し破綻。 |
| Q10 | 「資源下限制約下の最大収量」は管理に使えるのか | 上限 f_max の設定に依存する量なので、そのまま管理基準には使えない。使うなら f_max を生物学的根拠（例：漁具・努力量の物理的上限）で設定する必要がある。 |

---

## 4. 9/7 までのタスク（今日 7/29 から約5.7週間）

主結論が**構造的でパラメータに依存しない**ため、追加の重い再推定は不要。優先度は「定理の足場固め」＞「レジーム差の主張の補強」。

### 第1週（7/29–8/4）— 定理の足場固め【最優先】
1. **文献確認**：Volterra の原理／多種系MSY（May et al. 1979 Science; Legović, Klanjšček & Geček 2010 Ecol. Modelling 他）を実際に読み、本研究の差分を1段落で書けるようにする。Q1に答えられないと発表全体が「既知の再発見」に見える。
2. **定理を自分の手で再導出**：2×2系への分解 → 多重線形性 → 頂点最大、を白紙から書けるように。口頭で式変形を1回説明できることが目標。
3. **教授へ報告**：主軸を構造定理に格上げする件と、要旨との整合（§1の表）を確認。

### 第2週（8/5–8/11）— 数値の再現性と限界の明示
4. **検証コードの本採用**：平衡収量の構造検証（∂x_eq/∂f_x=0・多重線形性・頂点性）を `現行コード/msy/` の正式スクリプトに昇格させ、`docs/research_log.md` に Phase 14 として記録。スライドの数値表はここから引く。
5. **時間平均収量版の挙動確認**：厳密な多重線形性は平衡収量の性質。有限T の時間平均収量が f についてどれだけ非線形かを数値で測り、留保の言い方を確定する。

### 第3週（8/12–8/18）— 補強解析（余力に応じて）
6. **識別性の定量化**（Q3対策）：推定解でのヘッセ行列 or 残差ヤコビアンの特異値分解を取り、(C, L) → (C·λ, L/λ) 方向がほぼゼロ特異値であることを示す。図1枚で「和 vs 積」の主張が数学的に固まる。
7. **分割年の置換検定**（Q6対策）：分割年を 2006–2024 の各年に動かしてNRMSEやパラメータ差の分布を作り、2017分割の特異性を見る。

### 第4週（8/19–8/25）— スライド作成
8. 図の発表用作り直し（文字サイズ・日本語ラベル・1図1メッセージ）。既存PNGは情報量が多すぎるので分解する。
9. スライド初版を通しで作る。

### 第5週（8/26–9/1）
10. 通し練習（12分厳守）＋ 教授レビュー ＋ 想定問答の詰め。

### 第6週（9/2–9/6）
11. 修正・予備スライド整備・リハーサル2回。

### やらないこと（明示的に切る）
- 種内競争項（16変数）の再導入 → LM期8点では識別性が崩壊する実績があり、5週間では危険。Future Work として明言する。
- ブートストラップCI → 主結論に不要。レジーム差を強く主張する構成に変えた場合のみ着手。

---

## 参照
- `MSY.md`（4モードの定義・診断結果・総括）
- `行列式結果.md`（平衡点の手計算4パターン）
- `docs/research_log.md` Phase 8–13b（実験経緯の一次記録）
- `要旨準備.md`（提出前の検討過程。障害A/B/Cの整理）
- 図：`現行コード/msy/outputs/マアジ版/`（自由推定・制約10var・制約8var）、`現行コード/catch_msy/outputs/`
